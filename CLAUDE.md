# 현대 글로비스 역직구 파트 메뉴얼 — 인수인계

물류센터 현장 작업 가이드. 아르바이트/신입이 처음 보고도 따라할 수 있도록 만든
**단일 HTML 파일**(`index.html`, 약 72KB). 외부 의존성은 Pretendard 웹폰트 CDN 하나뿐이며,
폰트가 차단돼도 시스템 폰트로 정상 동작한다.

GitHub 공개 저장소 + GitHub Pages로 배포. 현장 PC에서는 바탕화면 바로가기로 연다.
(현장 PC는 보안 정책상 **파일 다운로드가 차단**돼 있어 설치형·이미지형은 불가.)

---

## 1. 파일 구조

`index.html` 한 파일에 HTML + CSS + JS + 모든 삽화(인라인 SVG)가 들어 있다.

```
<style>          디자인 토큰, 레이아웃, 반응형
#home            홈 메뉴 (가이드 3개 카드)
#guide           가이드 뷰 (기본 display:none)
  .gtop > #back  ← 메뉴 버튼
  #gTitle,#gFlow 제목 / 순서 요약
  .player
    .stage       .scene 16개 (전 가이드분이 전부 DOM에 존재, 활성 1개만 .on)
    .side
      .cap       #capTitle, #capText
      .track     #fill  (진행 바)
      .ctrl      #prev #play #next .dots
  #back2         처음 화면으로 돌아가기
<script>         바코드 생성기 + 가이드 데이터 + 라우팅
<style>          애니메이션 keyframes
```

## 2. 가이드 데이터 모델

JS 안의 `G` 객체 하나가 전체 콘텐츠를 담는다. **여기만 고치면 문구가 바뀐다.**

```js
var G = {
  L: { title, flow, steps:[ {k,t,p,d}, ... ] },   // 라벨 출력·부착
  M: { ... },                                      // 맵핑 작업
  R: { ... }                                       // 라벨 용지 교체
};
```

| 키 | 의미 |
|---|---|
| `k` | 단계 표기. `'01'~'05'` 또는 예외 `'A'`,`'B'` (A/B는 도트가 빨간색으로 렌더) |
| `t` | 캡션 제목 |
| `p` | 캡션 본문. `<em>`으로 감싼 부분이 노란 강조로 표시됨 |
| `d` | 자동 재생 시 노출 시간(ms) |

`steps` 배열의 **n번째 항목은 `<가이드키><n>` id를 가진 씬과 1:1로 대응**한다.
`L`은 `L0`~`L5`, `M`은 `M0`~`M4`, `R`은 `R0`~`R4`.
씬을 추가/삭제하면 id를 0부터 다시 붙이고 `steps` 길이를 맞춰야 한다.

## 3. 삽화 (SVG) 규칙

- 모든 씬은 `viewBox="0 0 400 320"` 고정. 이 좌표계를 벗어나지 말 것
- 씬 마크업 형태:
  ```html
  <div class="scene" id="M1" data-g="M">
    <div class="tag">STEP 02</div>      <!-- 예외는 class="tag alt" -->
    <svg viewBox="0 0 400 320" role="img" aria-label="...">…</svg>
  </div>
  ```
- SVG 안의 한글 텍스트는 `font-family="ui-monospace,monospace"`를 쓴다
  (한글은 폴백 폰트로 렌더되며, 숫자·영문과 톤이 맞는다)
- **사진을 쓰지 않는다.** 전부 직접 그린 벡터 도형
- **실제 운영 데이터를 쓰지 않는다.** 팔레트 번호는 `260101-TEST-001` 형식,
  GL번호는 `GL260101000123`, 송장은 `5702-1234-5678` 등 전부 임의값.
  실제 수취인 이름·주소·전화번호는 회색 막대로 처리

### 바코드
직접 그리지 말고 플레이스홀더를 두면 로드 시 JS가 채운다.

```html
<g data-bar data-x="36" data-y="70" data-w="96" data-h="26"
   data-dir="h" data-seed="71" fill="#14171A"></g>
```
`data-dir`은 `h`(세로 막대가 가로로 나열) 또는 `v`(90도 회전).
`data-seed`는 결정적 난수 시드 — 같은 값이면 항상 같은 무늬가 나온다.

### 애니메이션
활성 씬(`.scene.on`)에서만 동작한다. 요소에 `data-anim` 속성을 붙인다.

| 값 | 용도 |
|---|---|
| `sweep` | 스캐너 빔이 바코드를 훑는다. **`style="--sw:96px"`로 바코드 폭을 반드시 지정** (미지정 시 112px) |
| `blink` | 입력칸 커서 깜빡임 |
| `rise` | 프린터에서 라벨이 올라옴 |
| `ping` | 경고음 파동 |
| `drop` | 화물이 바닥에 놓임 |

`prefers-reduced-motion`에서 전부 정지한다.

## 4. 디자인 토큰

```
--bg     #16191C   배경
--panel  #1F2429   카드/컨트롤
--edge   #2E353C   테두리
--paper  #FDFDFB   기기 화면·라벨 흰색
--muted  #8D959E   보조 텍스트
--yellow #FFC400   강조·안전색 (진행바 = 노랑/검정 사선 안전테이프)
--scan   #E4392F   스캔 빔·경고·예외
--ok     #22A96B   정상·완료
--kraft  #C79358   박스 골판지
```
UI 화면 재현 시 보라 `#6C5CE0`(활성 버튼·입력칸 테두리), 파랑 `#3B6BE0`(표 헤더·체크박스),
연파랑 `#DCEBFB`(선택된 행)을 쓴다. 실제 시스템 화면 색이다.

## 5. 라우팅 · 반응형

- 해시 라우팅: `#label` → L, `#mapping` → M, `#paper` → R, 없으면 홈
- 작업별로 바로가기를 따로 만들 수 있다 (라벨 PC는 `#label`, 맵핑 PC는 `#mapping`)
- 키보드: `←/→` 이전·다음, `Space` 재생/정지, `Esc` 홈
- 브레이크포인트
  - 기본(폰): 세로 1단
  - `700px+`: 폭 780px, 글자 확대
  - `1024px+`: 폭 1240px, **재생기가 좌(그림)/우(설명·컨트롤) 2단**으로 전환
- 전체화면 실행: 바로가기 대상에 `"…chrome.exe" --app=주소`

## 6. 콘텐츠 현황

### L — 라벨 출력 · 부착 (4 STEP + 예외 2)
1. 커서를 `바코드 검색` 칸에 두고 화물 용지의 상단 바코드 또는 우측 긴 바코드를 스캔
   (칸 안 안내문구는 `가송장 번호`)
2. 라벨 출력 — 맨 아래가 GL번호 (우측 하단에 GL 마지막 4자리가 큰 글씨로 한 번 더 표시됨)
3. 라벨 GL번호 = 화물 용지 GL번호 대조
4. 화물의 GL번호·바코드를 가리지 않게 부착 (물건이 바뀌었을 때 대조용)
- 예외 A: 경고음 + 화면 빨간 글씨 → 붙이지 말고 화물을 바닥에 내려놓는다
- 예외 B: 번호 불일치 → 붙이지 않고 재스캔, 그래도 다르면 담당자 확인

### M — 맵핑 작업 (4 STEP + 예외 1)
1. 팔레트 번호 체크 + `도착 국가 확인`이 보라색으로 켜졌는지 확인
   (꺼져 있으면 스캔해도 수량이 오르지 않는다)
2. 커서를 `운송장 번호` 칸에 두고 라벨의 **A 박스 바로 아래 바코드**를 스캔
   (맨 아래 GL번호 바코드가 아니다)
3. 스캔할 때마다 수량이 1씩 오르는지 확인
4. 파레트 완성되면 다음 팔레트 번호로 변경
- 예외 A: 잘못 담음 → `취소 스캔`을 켜고 그 화물을 재스캔.
  수량이 내려가며, 팔레트는 아무거나 지정해도 취소된다

### R — 라벨 용지 교체 (ZEBRA ZT411, 5 STEP)
1. 화면에 빨간 `미디어 없음`
2. 앞쪽 검정 손잡이를 잡고 앞으로 밀면서 위로 → 고정 해제
3. 스티커가 있는 면이 위로 오게 놓기 (용지는 접힌 팬폴드 묶음, 롤 아님)
4. 아래 상자에서 끌어올려 오른쪽 회색 브래킷 위로 넘기고 프린트헤드 밑을 통과 →
   기기 안쪽 벽에 밀착 → 검정 가이드로 폭 조절 → 손잡이 내려 고정
5. `일시중지 요청` 화면에서 PAUSE 버튼

## 7. 작업 시 주의

- **원본 사진은 참고만 하고 결과물에 넣지 않는다.** 전부 벡터로 다시 그린다
- 프린터 내부 삽화(R 가이드 STEP 02·04)는 실물 사진 비율을 재서 그린 것이라
  부품 위치를 임의로 옮기면 현장에서 못 알아본다. 수정 시 현장 확인 필요
- 문구는 짧게. 그림으로 전달되는 내용을 글로 반복하지 않는다
  (이 원칙 때문에 설명이 여러 번 삭제됐다)
- 회사명·업무 화면이 들어가므로 저장소 공개 범위는 사내 확인 후 결정

## 8. 미확정 / 후속 작업

- **적재 공정(파레트 포함 높이 150cm까지)** 은 별도 가이드로 분리 예정. 현재 미작성
- 이동 공정(사이드 구역으로 운반)은 단순해서 가이드 없음
- R 가이드 STEP 04의 검정 가이드 위치는 현장에서 한 번 더 확인 필요
- 맵핑 화면의 `송장 일치 확인`, `도착 공항 확인` 모드는 사용 여부 미확인
- **정규직용 메뉴얼은 별도 private 저장소로 만든다.** 현재 문서·가이드는 일용직
  현장 작업 절차라 공개 저장소 + Pages로 충분하지만, 정규직용은 시스템 권한·정산·
  예외 처리 재량 등 공개하면 안 되는 범위가 들어간다. 같은 저장소를 나중에 private으로
  돌려도 공개 이력이 남으므로 **처음부터 저장소를 나눠서 시작할 것**
